# 239



## 题干

试找到一个定义在$[0,1]$上的函数，它是定义在$[0,1]\to[0,1]$上的双射，但任意子区间不单调.



### 证明

$$
\begin{cases}
x,&x\in\mathbb{Q}\newline
1-x,& x\in \mathbb{R}/\mathbb{Q}
\end{cases}
$$

设无理数$a$，有理数$b$.

若原本有$b>a$，则：
$$
f(b)-f(a)=b-(1-a)=b-a-1<0
$$

并且，有理数点都是单调递增的.

故不具有单调性.